<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aymane.tchat">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <application
        android:name=".persistance.persistance"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Connexion">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Choix" />
        <activity android:name=".AffichageForum" />
        <activity android:name=".Chat" />
        <activity android:name=".ChoixNom" />
        <activity android:name=".Forum" />
        <activity android:name=".Parametrage"></activity>
        <activity android:name=".TwoNodesActivity" />
        <activity android:name=".MapActivity"
            android:hardwareAccelerated="false"></activity>
    </application>

</manifest>
